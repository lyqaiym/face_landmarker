package com.google.mediapipe.tasks.vision.facedetector;

import com.google.mediapipe.tasks.components.containers.Detection;

import java.util.List;

// Generated by com.google.auto.value.processor.AutoValueProcessor
final class AutoValue_FaceDetectorResult extends FaceDetectorResult {

  private final long timestampMs;

  private final List<Detection> detections;

  AutoValue_FaceDetectorResult(
      long timestampMs,
      List<Detection> detections) {
    this.timestampMs = timestampMs;
    if (detections == null) {
      throw new NullPointerException("Null detections");
    }
    this.detections = detections;
  }

  @Override
  public long timestampMs() {
    return timestampMs;
  }

  @Override
  public List<Detection> detections() {
    return detections;
  }

  @Override
  public String toString() {
    return "FaceDetectorResult{"
        + "timestampMs=" + timestampMs + ", "
        + "detections=" + detections
        + "}";
  }

  @Override
  public boolean equals(Object o) {
    if (o == this) {
      return true;
    }
    if (o instanceof FaceDetectorResult) {
      FaceDetectorResult that = (FaceDetectorResult) o;
      return this.timestampMs == that.timestampMs()
          && this.detections.equals(that.detections());
    }
    return false;
  }

  @Override
  public int hashCode() {
    int h$ = 1;
    h$ *= 1000003;
    h$ ^= (int) ((timestampMs >>> 32) ^ timestampMs);
    h$ *= 1000003;
    h$ ^= detections.hashCode();
    return h$;
  }

}
