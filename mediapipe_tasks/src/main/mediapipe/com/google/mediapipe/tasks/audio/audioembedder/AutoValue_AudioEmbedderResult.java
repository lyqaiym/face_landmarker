package com.google.mediapipe.tasks.audio.audioembedder;

import com.google.mediapipe.tasks.components.containers.EmbeddingResult;

import java.util.List;

// Generated by com.google.auto.value.processor.AutoValueProcessor
final class AutoValue_AudioEmbedderResult extends AudioEmbedderResult {

  private final List<EmbeddingResult> embeddingResults;

  private final long timestampMs;

  AutoValue_AudioEmbedderResult(
      List<EmbeddingResult> embeddingResults,
      long timestampMs) {
    if (embeddingResults == null) {
      throw new NullPointerException("Null embeddingResults");
    }
    this.embeddingResults = embeddingResults;
    this.timestampMs = timestampMs;
  }

  @Override
  public List<EmbeddingResult> embeddingResults() {
    return embeddingResults;
  }

  @Override
  public long timestampMs() {
    return timestampMs;
  }

  @Override
  public String toString() {
    return "AudioEmbedderResult{"
        + "embeddingResults=" + embeddingResults + ", "
        + "timestampMs=" + timestampMs
        + "}";
  }

  @Override
  public boolean equals(Object o) {
    if (o == this) {
      return true;
    }
    if (o instanceof AudioEmbedderResult) {
      AudioEmbedderResult that = (AudioEmbedderResult) o;
      return this.embeddingResults.equals(that.embeddingResults())
          && this.timestampMs == that.timestampMs();
    }
    return false;
  }

  @Override
  public int hashCode() {
    int h$ = 1;
    h$ *= 1000003;
    h$ ^= embeddingResults.hashCode();
    h$ *= 1000003;
    h$ ^= (int) ((timestampMs >>> 32) ^ timestampMs);
    return h$;
  }

}
