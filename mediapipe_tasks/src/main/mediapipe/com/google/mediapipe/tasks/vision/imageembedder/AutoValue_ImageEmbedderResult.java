package com.google.mediapipe.tasks.vision.imageembedder;

import com.google.mediapipe.tasks.components.containers.EmbeddingResult;

// Generated by com.google.auto.value.processor.AutoValueProcessor
final class AutoValue_ImageEmbedderResult extends ImageEmbedderResult {

  private final EmbeddingResult embeddingResult;

  private final long timestampMs;

  AutoValue_ImageEmbedderResult(
      EmbeddingResult embeddingResult,
      long timestampMs) {
    if (embeddingResult == null) {
      throw new NullPointerException("Null embeddingResult");
    }
    this.embeddingResult = embeddingResult;
    this.timestampMs = timestampMs;
  }

  @Override
  public EmbeddingResult embeddingResult() {
    return embeddingResult;
  }

  @Override
  public long timestampMs() {
    return timestampMs;
  }

  @Override
  public String toString() {
    return "ImageEmbedderResult{"
        + "embeddingResult=" + embeddingResult + ", "
        + "timestampMs=" + timestampMs
        + "}";
  }

  @Override
  public boolean equals(Object o) {
    if (o == this) {
      return true;
    }
    if (o instanceof ImageEmbedderResult) {
      ImageEmbedderResult that = (ImageEmbedderResult) o;
      return this.embeddingResult.equals(that.embeddingResult())
          && this.timestampMs == that.timestampMs();
    }
    return false;
  }

  @Override
  public int hashCode() {
    int h$ = 1;
    h$ *= 1000003;
    h$ ^= embeddingResult.hashCode();
    h$ *= 1000003;
    h$ ^= (int) ((timestampMs >>> 32) ^ timestampMs);
    return h$;
  }

}
