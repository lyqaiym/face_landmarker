package com.google.mediapipe.tasks.components.containers;

import java.util.List;
import java.util.Optional;

// Generated by com.google.auto.value.processor.AutoValueProcessor
final class AutoValue_EmbeddingResult extends EmbeddingResult {

  private final List<Embedding> embeddings;

  private final Optional<Long> timestampMs;

  AutoValue_EmbeddingResult(
      List<Embedding> embeddings,
      Optional<Long> timestampMs) {
    if (embeddings == null) {
      throw new NullPointerException("Null embeddings");
    }
    this.embeddings = embeddings;
    if (timestampMs == null) {
      throw new NullPointerException("Null timestampMs");
    }
    this.timestampMs = timestampMs;
  }

  @Override
  public List<Embedding> embeddings() {
    return embeddings;
  }

  @Override
  public Optional<Long> timestampMs() {
    return timestampMs;
  }

  @Override
  public String toString() {
    return "EmbeddingResult{"
        + "embeddings=" + embeddings + ", "
        + "timestampMs=" + timestampMs
        + "}";
  }

  @Override
  public boolean equals(Object o) {
    if (o == this) {
      return true;
    }
    if (o instanceof EmbeddingResult) {
      EmbeddingResult that = (EmbeddingResult) o;
      return this.embeddings.equals(that.embeddings())
          && this.timestampMs.equals(that.timestampMs());
    }
    return false;
  }

  @Override
  public int hashCode() {
    int h$ = 1;
    h$ *= 1000003;
    h$ ^= embeddings.hashCode();
    h$ *= 1000003;
    h$ ^= timestampMs.hashCode();
    return h$;
  }

}
