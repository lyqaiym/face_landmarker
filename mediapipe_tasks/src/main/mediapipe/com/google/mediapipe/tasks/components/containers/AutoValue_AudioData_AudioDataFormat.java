package com.google.mediapipe.tasks.components.containers;

// Generated by com.google.auto.value.processor.AutoValueProcessor
final class AutoValue_AudioData_AudioDataFormat extends AudioData.AudioDataFormat {

  private final int numOfChannels;

  private final float sampleRate;

  private AutoValue_AudioData_AudioDataFormat(
      int numOfChannels,
      float sampleRate) {
    this.numOfChannels = numOfChannels;
    this.sampleRate = sampleRate;
  }

  @Override
  public int getNumOfChannels() {
    return numOfChannels;
  }

  @Override
  public float getSampleRate() {
    return sampleRate;
  }

  @Override
  public String toString() {
    return "AudioDataFormat{"
        + "numOfChannels=" + numOfChannels + ", "
        + "sampleRate=" + sampleRate
        + "}";
  }

  @Override
  public boolean equals(Object o) {
    if (o == this) {
      return true;
    }
    if (o instanceof AudioData.AudioDataFormat) {
      AudioData.AudioDataFormat that = (AudioData.AudioDataFormat) o;
      return this.numOfChannels == that.getNumOfChannels()
          && Float.floatToIntBits(this.sampleRate) == Float.floatToIntBits(that.getSampleRate());
    }
    return false;
  }

  @Override
  public int hashCode() {
    int h$ = 1;
    h$ *= 1000003;
    h$ ^= numOfChannels;
    h$ *= 1000003;
    h$ ^= Float.floatToIntBits(sampleRate);
    return h$;
  }

  static final class Builder extends AudioData.AudioDataFormat.Builder {
    private Integer numOfChannels;
    private Float sampleRate;
    Builder() {
    }
    @Override
    public AudioData.AudioDataFormat.Builder setNumOfChannels(int numOfChannels) {
      this.numOfChannels = numOfChannels;
      return this;
    }
    @Override
    public AudioData.AudioDataFormat.Builder setSampleRate(float sampleRate) {
      this.sampleRate = sampleRate;
      return this;
    }
    @Override
    AudioData.AudioDataFormat autoBuild() {
      String missing = "";
      if (this.numOfChannels == null) {
        missing += " numOfChannels";
      }
      if (this.sampleRate == null) {
        missing += " sampleRate";
      }
      if (!missing.isEmpty()) {
        throw new IllegalStateException("Missing required properties:" + missing);
      }
      return new AutoValue_AudioData_AudioDataFormat(
          this.numOfChannels,
          this.sampleRate);
    }
  }

}
