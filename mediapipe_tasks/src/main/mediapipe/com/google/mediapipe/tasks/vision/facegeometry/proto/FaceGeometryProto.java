// Generated by the protocol buffer compiler.  DO NOT EDIT!
// source: mediapipe/tasks/cc/vision/face_geometry/proto/face_geometry.proto

package com.google.mediapipe.tasks.vision.facegeometry.proto;

public final class FaceGeometryProto {
  private FaceGeometryProto() {}
  public static void registerAllExtensions(
      com.google.protobuf.ExtensionRegistryLite registry) {
  }
  public interface FaceGeometryOrBuilder extends
      // @@protoc_insertion_point(interface_extends:mediapipe.tasks.vision.face_geometry.proto.FaceGeometry)
      com.google.protobuf.MessageLiteOrBuilder {

    /**
     * <pre>
     * Defines a mesh surface for a face. The face mesh vertex IDs are the same as
     * the face landmark IDs.
     * XYZ coordinates exist in the right-handed Metric 3D space configured by an
     * environment. UV coordinates are taken from the canonical face mesh model.
     * XY coordinates are guaranteed to match the screen positions of
     * the input face landmarks after (1) being multiplied by the face pose
     * transformation matrix and then (2) being projected with a perspective
     * camera matrix of the same environment.
     * NOTE: the triangular topology of the face mesh is only useful when derived
     *       from the 468 face landmarks, not from the 6 face detection landmarks
     *       (keypoints). The former don't cover the entire face and this mesh is
     *       defined here only to comply with the API. It should be considered as
     *       a placeholder and/or for debugging purposes.
     *       Use the face geometry derived from the face detection landmarks
     *       (keypoints) for the face pose transformation matrix, not the mesh.
     * </pre>
     *
     * <code>optional .mediapipe.tasks.vision.face_geometry.proto.Mesh3d mesh = 1;</code>
     * @return Whether the mesh field is set.
     */
    boolean hasMesh();
    /**
     * <pre>
     * Defines a mesh surface for a face. The face mesh vertex IDs are the same as
     * the face landmark IDs.
     * XYZ coordinates exist in the right-handed Metric 3D space configured by an
     * environment. UV coordinates are taken from the canonical face mesh model.
     * XY coordinates are guaranteed to match the screen positions of
     * the input face landmarks after (1) being multiplied by the face pose
     * transformation matrix and then (2) being projected with a perspective
     * camera matrix of the same environment.
     * NOTE: the triangular topology of the face mesh is only useful when derived
     *       from the 468 face landmarks, not from the 6 face detection landmarks
     *       (keypoints). The former don't cover the entire face and this mesh is
     *       defined here only to comply with the API. It should be considered as
     *       a placeholder and/or for debugging purposes.
     *       Use the face geometry derived from the face detection landmarks
     *       (keypoints) for the face pose transformation matrix, not the mesh.
     * </pre>
     *
     * <code>optional .mediapipe.tasks.vision.face_geometry.proto.Mesh3d mesh = 1;</code>
     * @return The mesh.
     */
    Mesh3DProto.Mesh3d getMesh();

    /**
     * <pre>
     * Defines a face pose transformation matrix, which provides mapping from
     * the static canonical face model to the runtime face. Tries to distinguish
     * a head pose change from a facial expression change and to only reflect the
     * former.
     * Is a 4x4 matrix and contains only the following components:
     *   * Uniform scale
     *   * Rotation
     *   * Translation
     * The last row is guaranteed to be `[0 0 0 1]`.
     * </pre>
     *
     * <code>optional .mediapipe.MatrixData pose_transform_matrix = 2;</code>
     * @return Whether the poseTransformMatrix field is set.
     */
    boolean hasPoseTransformMatrix();
    /**
     * <pre>
     * Defines a face pose transformation matrix, which provides mapping from
     * the static canonical face model to the runtime face. Tries to distinguish
     * a head pose change from a facial expression change and to only reflect the
     * former.
     * Is a 4x4 matrix and contains only the following components:
     *   * Uniform scale
     *   * Rotation
     *   * Translation
     * The last row is guaranteed to be `[0 0 0 1]`.
     * </pre>
     *
     * <code>optional .mediapipe.MatrixData pose_transform_matrix = 2;</code>
     * @return The poseTransformMatrix.
     */
    com.google.mediapipe.formats.proto.MatrixDataProto.MatrixData getPoseTransformMatrix();
  }
  /**
   * <pre>
   * Defines the face geometry pipeline estimation result format.
   * </pre>
   *
   * Protobuf type {@code mediapipe.tasks.vision.face_geometry.proto.FaceGeometry}
   */
  public  static final class FaceGeometry extends
      com.google.protobuf.GeneratedMessageLite<
          FaceGeometry, FaceGeometry.Builder> implements
      // @@protoc_insertion_point(message_implements:mediapipe.tasks.vision.face_geometry.proto.FaceGeometry)
      FaceGeometryOrBuilder {
    private FaceGeometry() {
    }
    private int bitField0_;
    public static final int MESH_FIELD_NUMBER = 1;
    private Mesh3DProto.Mesh3d mesh_;
    /**
     * <pre>
     * Defines a mesh surface for a face. The face mesh vertex IDs are the same as
     * the face landmark IDs.
     * XYZ coordinates exist in the right-handed Metric 3D space configured by an
     * environment. UV coordinates are taken from the canonical face mesh model.
     * XY coordinates are guaranteed to match the screen positions of
     * the input face landmarks after (1) being multiplied by the face pose
     * transformation matrix and then (2) being projected with a perspective
     * camera matrix of the same environment.
     * NOTE: the triangular topology of the face mesh is only useful when derived
     *       from the 468 face landmarks, not from the 6 face detection landmarks
     *       (keypoints). The former don't cover the entire face and this mesh is
     *       defined here only to comply with the API. It should be considered as
     *       a placeholder and/or for debugging purposes.
     *       Use the face geometry derived from the face detection landmarks
     *       (keypoints) for the face pose transformation matrix, not the mesh.
     * </pre>
     *
     * <code>optional .mediapipe.tasks.vision.face_geometry.proto.Mesh3d mesh = 1;</code>
     */
    @Override
    public boolean hasMesh() {
      return ((bitField0_ & 0x00000001) != 0);
    }
    /**
     * <pre>
     * Defines a mesh surface for a face. The face mesh vertex IDs are the same as
     * the face landmark IDs.
     * XYZ coordinates exist in the right-handed Metric 3D space configured by an
     * environment. UV coordinates are taken from the canonical face mesh model.
     * XY coordinates are guaranteed to match the screen positions of
     * the input face landmarks after (1) being multiplied by the face pose
     * transformation matrix and then (2) being projected with a perspective
     * camera matrix of the same environment.
     * NOTE: the triangular topology of the face mesh is only useful when derived
     *       from the 468 face landmarks, not from the 6 face detection landmarks
     *       (keypoints). The former don't cover the entire face and this mesh is
     *       defined here only to comply with the API. It should be considered as
     *       a placeholder and/or for debugging purposes.
     *       Use the face geometry derived from the face detection landmarks
     *       (keypoints) for the face pose transformation matrix, not the mesh.
     * </pre>
     *
     * <code>optional .mediapipe.tasks.vision.face_geometry.proto.Mesh3d mesh = 1;</code>
     */
    @Override
    public Mesh3DProto.Mesh3d getMesh() {
      return mesh_ == null ? Mesh3DProto.Mesh3d.getDefaultInstance() : mesh_;
    }
    /**
     * <pre>
     * Defines a mesh surface for a face. The face mesh vertex IDs are the same as
     * the face landmark IDs.
     * XYZ coordinates exist in the right-handed Metric 3D space configured by an
     * environment. UV coordinates are taken from the canonical face mesh model.
     * XY coordinates are guaranteed to match the screen positions of
     * the input face landmarks after (1) being multiplied by the face pose
     * transformation matrix and then (2) being projected with a perspective
     * camera matrix of the same environment.
     * NOTE: the triangular topology of the face mesh is only useful when derived
     *       from the 468 face landmarks, not from the 6 face detection landmarks
     *       (keypoints). The former don't cover the entire face and this mesh is
     *       defined here only to comply with the API. It should be considered as
     *       a placeholder and/or for debugging purposes.
     *       Use the face geometry derived from the face detection landmarks
     *       (keypoints) for the face pose transformation matrix, not the mesh.
     * </pre>
     *
     * <code>optional .mediapipe.tasks.vision.face_geometry.proto.Mesh3d mesh = 1;</code>
     */
    private void setMesh(Mesh3DProto.Mesh3d value) {
      value.getClass();
  mesh_ = value;
      bitField0_ |= 0x00000001;
      }
    /**
     * <pre>
     * Defines a mesh surface for a face. The face mesh vertex IDs are the same as
     * the face landmark IDs.
     * XYZ coordinates exist in the right-handed Metric 3D space configured by an
     * environment. UV coordinates are taken from the canonical face mesh model.
     * XY coordinates are guaranteed to match the screen positions of
     * the input face landmarks after (1) being multiplied by the face pose
     * transformation matrix and then (2) being projected with a perspective
     * camera matrix of the same environment.
     * NOTE: the triangular topology of the face mesh is only useful when derived
     *       from the 468 face landmarks, not from the 6 face detection landmarks
     *       (keypoints). The former don't cover the entire face and this mesh is
     *       defined here only to comply with the API. It should be considered as
     *       a placeholder and/or for debugging purposes.
     *       Use the face geometry derived from the face detection landmarks
     *       (keypoints) for the face pose transformation matrix, not the mesh.
     * </pre>
     *
     * <code>optional .mediapipe.tasks.vision.face_geometry.proto.Mesh3d mesh = 1;</code>
     */
    @SuppressWarnings({"ReferenceEquality"})
    private void mergeMesh(Mesh3DProto.Mesh3d value) {
      value.getClass();
  if (mesh_ != null &&
          mesh_ != Mesh3DProto.Mesh3d.getDefaultInstance()) {
        mesh_ =
          Mesh3DProto.Mesh3d.newBuilder(mesh_).mergeFrom(value).buildPartial();
      } else {
        mesh_ = value;
      }
      bitField0_ |= 0x00000001;
    }
    /**
     * <pre>
     * Defines a mesh surface for a face. The face mesh vertex IDs are the same as
     * the face landmark IDs.
     * XYZ coordinates exist in the right-handed Metric 3D space configured by an
     * environment. UV coordinates are taken from the canonical face mesh model.
     * XY coordinates are guaranteed to match the screen positions of
     * the input face landmarks after (1) being multiplied by the face pose
     * transformation matrix and then (2) being projected with a perspective
     * camera matrix of the same environment.
     * NOTE: the triangular topology of the face mesh is only useful when derived
     *       from the 468 face landmarks, not from the 6 face detection landmarks
     *       (keypoints). The former don't cover the entire face and this mesh is
     *       defined here only to comply with the API. It should be considered as
     *       a placeholder and/or for debugging purposes.
     *       Use the face geometry derived from the face detection landmarks
     *       (keypoints) for the face pose transformation matrix, not the mesh.
     * </pre>
     *
     * <code>optional .mediapipe.tasks.vision.face_geometry.proto.Mesh3d mesh = 1;</code>
     */
    private void clearMesh() {  mesh_ = null;
      bitField0_ = (bitField0_ & ~0x00000001);
    }

    public static final int POSE_TRANSFORM_MATRIX_FIELD_NUMBER = 2;
    private com.google.mediapipe.formats.proto.MatrixDataProto.MatrixData poseTransformMatrix_;
    /**
     * <pre>
     * Defines a face pose transformation matrix, which provides mapping from
     * the static canonical face model to the runtime face. Tries to distinguish
     * a head pose change from a facial expression change and to only reflect the
     * former.
     * Is a 4x4 matrix and contains only the following components:
     *   * Uniform scale
     *   * Rotation
     *   * Translation
     * The last row is guaranteed to be `[0 0 0 1]`.
     * </pre>
     *
     * <code>optional .mediapipe.MatrixData pose_transform_matrix = 2;</code>
     */
    @Override
    public boolean hasPoseTransformMatrix() {
      return ((bitField0_ & 0x00000002) != 0);
    }
    /**
     * <pre>
     * Defines a face pose transformation matrix, which provides mapping from
     * the static canonical face model to the runtime face. Tries to distinguish
     * a head pose change from a facial expression change and to only reflect the
     * former.
     * Is a 4x4 matrix and contains only the following components:
     *   * Uniform scale
     *   * Rotation
     *   * Translation
     * The last row is guaranteed to be `[0 0 0 1]`.
     * </pre>
     *
     * <code>optional .mediapipe.MatrixData pose_transform_matrix = 2;</code>
     */
    @Override
    public com.google.mediapipe.formats.proto.MatrixDataProto.MatrixData getPoseTransformMatrix() {
      return poseTransformMatrix_ == null ? com.google.mediapipe.formats.proto.MatrixDataProto.MatrixData.getDefaultInstance() : poseTransformMatrix_;
    }
    /**
     * <pre>
     * Defines a face pose transformation matrix, which provides mapping from
     * the static canonical face model to the runtime face. Tries to distinguish
     * a head pose change from a facial expression change and to only reflect the
     * former.
     * Is a 4x4 matrix and contains only the following components:
     *   * Uniform scale
     *   * Rotation
     *   * Translation
     * The last row is guaranteed to be `[0 0 0 1]`.
     * </pre>
     *
     * <code>optional .mediapipe.MatrixData pose_transform_matrix = 2;</code>
     */
    private void setPoseTransformMatrix(com.google.mediapipe.formats.proto.MatrixDataProto.MatrixData value) {
      value.getClass();
  poseTransformMatrix_ = value;
      bitField0_ |= 0x00000002;
      }
    /**
     * <pre>
     * Defines a face pose transformation matrix, which provides mapping from
     * the static canonical face model to the runtime face. Tries to distinguish
     * a head pose change from a facial expression change and to only reflect the
     * former.
     * Is a 4x4 matrix and contains only the following components:
     *   * Uniform scale
     *   * Rotation
     *   * Translation
     * The last row is guaranteed to be `[0 0 0 1]`.
     * </pre>
     *
     * <code>optional .mediapipe.MatrixData pose_transform_matrix = 2;</code>
     */
    @SuppressWarnings({"ReferenceEquality"})
    private void mergePoseTransformMatrix(com.google.mediapipe.formats.proto.MatrixDataProto.MatrixData value) {
      value.getClass();
  if (poseTransformMatrix_ != null &&
          poseTransformMatrix_ != com.google.mediapipe.formats.proto.MatrixDataProto.MatrixData.getDefaultInstance()) {
        poseTransformMatrix_ =
          com.google.mediapipe.formats.proto.MatrixDataProto.MatrixData.newBuilder(poseTransformMatrix_).mergeFrom(value).buildPartial();
      } else {
        poseTransformMatrix_ = value;
      }
      bitField0_ |= 0x00000002;
    }
    /**
     * <pre>
     * Defines a face pose transformation matrix, which provides mapping from
     * the static canonical face model to the runtime face. Tries to distinguish
     * a head pose change from a facial expression change and to only reflect the
     * former.
     * Is a 4x4 matrix and contains only the following components:
     *   * Uniform scale
     *   * Rotation
     *   * Translation
     * The last row is guaranteed to be `[0 0 0 1]`.
     * </pre>
     *
     * <code>optional .mediapipe.MatrixData pose_transform_matrix = 2;</code>
     */
    private void clearPoseTransformMatrix() {  poseTransformMatrix_ = null;
      bitField0_ = (bitField0_ & ~0x00000002);
    }

    public static FaceGeometry parseFrom(
        java.nio.ByteBuffer data)
        throws com.google.protobuf.InvalidProtocolBufferException {
      return com.google.protobuf.GeneratedMessageLite.parseFrom(
          DEFAULT_INSTANCE, data);
    }
    public static FaceGeometry parseFrom(
        java.nio.ByteBuffer data,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws com.google.protobuf.InvalidProtocolBufferException {
      return com.google.protobuf.GeneratedMessageLite.parseFrom(
          DEFAULT_INSTANCE, data, extensionRegistry);
    }
    public static FaceGeometry parseFrom(
        com.google.protobuf.ByteString data)
        throws com.google.protobuf.InvalidProtocolBufferException {
      return com.google.protobuf.GeneratedMessageLite.parseFrom(
          DEFAULT_INSTANCE, data);
    }
    public static FaceGeometry parseFrom(
        com.google.protobuf.ByteString data,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws com.google.protobuf.InvalidProtocolBufferException {
      return com.google.protobuf.GeneratedMessageLite.parseFrom(
          DEFAULT_INSTANCE, data, extensionRegistry);
    }
    public static FaceGeometry parseFrom(byte[] data)
        throws com.google.protobuf.InvalidProtocolBufferException {
      return com.google.protobuf.GeneratedMessageLite.parseFrom(
          DEFAULT_INSTANCE, data);
    }
    public static FaceGeometry parseFrom(
        byte[] data,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws com.google.protobuf.InvalidProtocolBufferException {
      return com.google.protobuf.GeneratedMessageLite.parseFrom(
          DEFAULT_INSTANCE, data, extensionRegistry);
    }
    public static FaceGeometry parseFrom(java.io.InputStream input)
        throws java.io.IOException {
      return com.google.protobuf.GeneratedMessageLite.parseFrom(
          DEFAULT_INSTANCE, input);
    }
    public static FaceGeometry parseFrom(
        java.io.InputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws java.io.IOException {
      return com.google.protobuf.GeneratedMessageLite.parseFrom(
          DEFAULT_INSTANCE, input, extensionRegistry);
    }
    public static FaceGeometry parseDelimitedFrom(java.io.InputStream input)
        throws java.io.IOException {
      return parseDelimitedFrom(DEFAULT_INSTANCE, input);
    }
    public static FaceGeometry parseDelimitedFrom(
        java.io.InputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws java.io.IOException {
      return parseDelimitedFrom(DEFAULT_INSTANCE, input, extensionRegistry);
    }
    public static FaceGeometry parseFrom(
        com.google.protobuf.CodedInputStream input)
        throws java.io.IOException {
      return com.google.protobuf.GeneratedMessageLite.parseFrom(
          DEFAULT_INSTANCE, input);
    }
    public static FaceGeometry parseFrom(
        com.google.protobuf.CodedInputStream input,
        com.google.protobuf.ExtensionRegistryLite extensionRegistry)
        throws java.io.IOException {
      return com.google.protobuf.GeneratedMessageLite.parseFrom(
          DEFAULT_INSTANCE, input, extensionRegistry);
    }

    public static Builder newBuilder() {
      return (Builder) DEFAULT_INSTANCE.createBuilder();
    }
    public static Builder newBuilder(FaceGeometry prototype) {
      return (Builder) DEFAULT_INSTANCE.createBuilder(prototype);
    }

    /**
     * <pre>
     * Defines the face geometry pipeline estimation result format.
     * </pre>
     *
     * Protobuf type {@code mediapipe.tasks.vision.face_geometry.proto.FaceGeometry}
     */
    public static final class Builder extends
        com.google.protobuf.GeneratedMessageLite.Builder<
          FaceGeometry, Builder> implements
        // @@protoc_insertion_point(builder_implements:mediapipe.tasks.vision.face_geometry.proto.FaceGeometry)
        FaceGeometryOrBuilder {
      // Construct using com.google.mediapipe.tasks.vision.facegeometry.proto.FaceGeometryProto.FaceGeometry.newBuilder()
      private Builder() {
        super(DEFAULT_INSTANCE);
      }


      /**
       * <pre>
       * Defines a mesh surface for a face. The face mesh vertex IDs are the same as
       * the face landmark IDs.
       * XYZ coordinates exist in the right-handed Metric 3D space configured by an
       * environment. UV coordinates are taken from the canonical face mesh model.
       * XY coordinates are guaranteed to match the screen positions of
       * the input face landmarks after (1) being multiplied by the face pose
       * transformation matrix and then (2) being projected with a perspective
       * camera matrix of the same environment.
       * NOTE: the triangular topology of the face mesh is only useful when derived
       *       from the 468 face landmarks, not from the 6 face detection landmarks
       *       (keypoints). The former don't cover the entire face and this mesh is
       *       defined here only to comply with the API. It should be considered as
       *       a placeholder and/or for debugging purposes.
       *       Use the face geometry derived from the face detection landmarks
       *       (keypoints) for the face pose transformation matrix, not the mesh.
       * </pre>
       *
       * <code>optional .mediapipe.tasks.vision.face_geometry.proto.Mesh3d mesh = 1;</code>
       */
      @Override
      public boolean hasMesh() {
        return instance.hasMesh();
      }
      /**
       * <pre>
       * Defines a mesh surface for a face. The face mesh vertex IDs are the same as
       * the face landmark IDs.
       * XYZ coordinates exist in the right-handed Metric 3D space configured by an
       * environment. UV coordinates are taken from the canonical face mesh model.
       * XY coordinates are guaranteed to match the screen positions of
       * the input face landmarks after (1) being multiplied by the face pose
       * transformation matrix and then (2) being projected with a perspective
       * camera matrix of the same environment.
       * NOTE: the triangular topology of the face mesh is only useful when derived
       *       from the 468 face landmarks, not from the 6 face detection landmarks
       *       (keypoints). The former don't cover the entire face and this mesh is
       *       defined here only to comply with the API. It should be considered as
       *       a placeholder and/or for debugging purposes.
       *       Use the face geometry derived from the face detection landmarks
       *       (keypoints) for the face pose transformation matrix, not the mesh.
       * </pre>
       *
       * <code>optional .mediapipe.tasks.vision.face_geometry.proto.Mesh3d mesh = 1;</code>
       */
      @Override
      public Mesh3DProto.Mesh3d getMesh() {
        return instance.getMesh();
      }
      /**
       * <pre>
       * Defines a mesh surface for a face. The face mesh vertex IDs are the same as
       * the face landmark IDs.
       * XYZ coordinates exist in the right-handed Metric 3D space configured by an
       * environment. UV coordinates are taken from the canonical face mesh model.
       * XY coordinates are guaranteed to match the screen positions of
       * the input face landmarks after (1) being multiplied by the face pose
       * transformation matrix and then (2) being projected with a perspective
       * camera matrix of the same environment.
       * NOTE: the triangular topology of the face mesh is only useful when derived
       *       from the 468 face landmarks, not from the 6 face detection landmarks
       *       (keypoints). The former don't cover the entire face and this mesh is
       *       defined here only to comply with the API. It should be considered as
       *       a placeholder and/or for debugging purposes.
       *       Use the face geometry derived from the face detection landmarks
       *       (keypoints) for the face pose transformation matrix, not the mesh.
       * </pre>
       *
       * <code>optional .mediapipe.tasks.vision.face_geometry.proto.Mesh3d mesh = 1;</code>
       */
      public Builder setMesh(Mesh3DProto.Mesh3d value) {
        copyOnWrite();
        instance.setMesh(value);
        return this;
        }
      /**
       * <pre>
       * Defines a mesh surface for a face. The face mesh vertex IDs are the same as
       * the face landmark IDs.
       * XYZ coordinates exist in the right-handed Metric 3D space configured by an
       * environment. UV coordinates are taken from the canonical face mesh model.
       * XY coordinates are guaranteed to match the screen positions of
       * the input face landmarks after (1) being multiplied by the face pose
       * transformation matrix and then (2) being projected with a perspective
       * camera matrix of the same environment.
       * NOTE: the triangular topology of the face mesh is only useful when derived
       *       from the 468 face landmarks, not from the 6 face detection landmarks
       *       (keypoints). The former don't cover the entire face and this mesh is
       *       defined here only to comply with the API. It should be considered as
       *       a placeholder and/or for debugging purposes.
       *       Use the face geometry derived from the face detection landmarks
       *       (keypoints) for the face pose transformation matrix, not the mesh.
       * </pre>
       *
       * <code>optional .mediapipe.tasks.vision.face_geometry.proto.Mesh3d mesh = 1;</code>
       */
      public Builder setMesh(
          Mesh3DProto.Mesh3d.Builder builderForValue) {
        copyOnWrite();
        instance.setMesh(builderForValue.build());
        return this;
      }
      /**
       * <pre>
       * Defines a mesh surface for a face. The face mesh vertex IDs are the same as
       * the face landmark IDs.
       * XYZ coordinates exist in the right-handed Metric 3D space configured by an
       * environment. UV coordinates are taken from the canonical face mesh model.
       * XY coordinates are guaranteed to match the screen positions of
       * the input face landmarks after (1) being multiplied by the face pose
       * transformation matrix and then (2) being projected with a perspective
       * camera matrix of the same environment.
       * NOTE: the triangular topology of the face mesh is only useful when derived
       *       from the 468 face landmarks, not from the 6 face detection landmarks
       *       (keypoints). The former don't cover the entire face and this mesh is
       *       defined here only to comply with the API. It should be considered as
       *       a placeholder and/or for debugging purposes.
       *       Use the face geometry derived from the face detection landmarks
       *       (keypoints) for the face pose transformation matrix, not the mesh.
       * </pre>
       *
       * <code>optional .mediapipe.tasks.vision.face_geometry.proto.Mesh3d mesh = 1;</code>
       */
      public Builder mergeMesh(Mesh3DProto.Mesh3d value) {
        copyOnWrite();
        instance.mergeMesh(value);
        return this;
      }
      /**
       * <pre>
       * Defines a mesh surface for a face. The face mesh vertex IDs are the same as
       * the face landmark IDs.
       * XYZ coordinates exist in the right-handed Metric 3D space configured by an
       * environment. UV coordinates are taken from the canonical face mesh model.
       * XY coordinates are guaranteed to match the screen positions of
       * the input face landmarks after (1) being multiplied by the face pose
       * transformation matrix and then (2) being projected with a perspective
       * camera matrix of the same environment.
       * NOTE: the triangular topology of the face mesh is only useful when derived
       *       from the 468 face landmarks, not from the 6 face detection landmarks
       *       (keypoints). The former don't cover the entire face and this mesh is
       *       defined here only to comply with the API. It should be considered as
       *       a placeholder and/or for debugging purposes.
       *       Use the face geometry derived from the face detection landmarks
       *       (keypoints) for the face pose transformation matrix, not the mesh.
       * </pre>
       *
       * <code>optional .mediapipe.tasks.vision.face_geometry.proto.Mesh3d mesh = 1;</code>
       */
      public Builder clearMesh() {  copyOnWrite();
        instance.clearMesh();
        return this;
      }

      /**
       * <pre>
       * Defines a face pose transformation matrix, which provides mapping from
       * the static canonical face model to the runtime face. Tries to distinguish
       * a head pose change from a facial expression change and to only reflect the
       * former.
       * Is a 4x4 matrix and contains only the following components:
       *   * Uniform scale
       *   * Rotation
       *   * Translation
       * The last row is guaranteed to be `[0 0 0 1]`.
       * </pre>
       *
       * <code>optional .mediapipe.MatrixData pose_transform_matrix = 2;</code>
       */
      @Override
      public boolean hasPoseTransformMatrix() {
        return instance.hasPoseTransformMatrix();
      }
      /**
       * <pre>
       * Defines a face pose transformation matrix, which provides mapping from
       * the static canonical face model to the runtime face. Tries to distinguish
       * a head pose change from a facial expression change and to only reflect the
       * former.
       * Is a 4x4 matrix and contains only the following components:
       *   * Uniform scale
       *   * Rotation
       *   * Translation
       * The last row is guaranteed to be `[0 0 0 1]`.
       * </pre>
       *
       * <code>optional .mediapipe.MatrixData pose_transform_matrix = 2;</code>
       */
      @Override
      public com.google.mediapipe.formats.proto.MatrixDataProto.MatrixData getPoseTransformMatrix() {
        return instance.getPoseTransformMatrix();
      }
      /**
       * <pre>
       * Defines a face pose transformation matrix, which provides mapping from
       * the static canonical face model to the runtime face. Tries to distinguish
       * a head pose change from a facial expression change and to only reflect the
       * former.
       * Is a 4x4 matrix and contains only the following components:
       *   * Uniform scale
       *   * Rotation
       *   * Translation
       * The last row is guaranteed to be `[0 0 0 1]`.
       * </pre>
       *
       * <code>optional .mediapipe.MatrixData pose_transform_matrix = 2;</code>
       */
      public Builder setPoseTransformMatrix(com.google.mediapipe.formats.proto.MatrixDataProto.MatrixData value) {
        copyOnWrite();
        instance.setPoseTransformMatrix(value);
        return this;
        }
      /**
       * <pre>
       * Defines a face pose transformation matrix, which provides mapping from
       * the static canonical face model to the runtime face. Tries to distinguish
       * a head pose change from a facial expression change and to only reflect the
       * former.
       * Is a 4x4 matrix and contains only the following components:
       *   * Uniform scale
       *   * Rotation
       *   * Translation
       * The last row is guaranteed to be `[0 0 0 1]`.
       * </pre>
       *
       * <code>optional .mediapipe.MatrixData pose_transform_matrix = 2;</code>
       */
      public Builder setPoseTransformMatrix(
          com.google.mediapipe.formats.proto.MatrixDataProto.MatrixData.Builder builderForValue) {
        copyOnWrite();
        instance.setPoseTransformMatrix(builderForValue.build());
        return this;
      }
      /**
       * <pre>
       * Defines a face pose transformation matrix, which provides mapping from
       * the static canonical face model to the runtime face. Tries to distinguish
       * a head pose change from a facial expression change and to only reflect the
       * former.
       * Is a 4x4 matrix and contains only the following components:
       *   * Uniform scale
       *   * Rotation
       *   * Translation
       * The last row is guaranteed to be `[0 0 0 1]`.
       * </pre>
       *
       * <code>optional .mediapipe.MatrixData pose_transform_matrix = 2;</code>
       */
      public Builder mergePoseTransformMatrix(com.google.mediapipe.formats.proto.MatrixDataProto.MatrixData value) {
        copyOnWrite();
        instance.mergePoseTransformMatrix(value);
        return this;
      }
      /**
       * <pre>
       * Defines a face pose transformation matrix, which provides mapping from
       * the static canonical face model to the runtime face. Tries to distinguish
       * a head pose change from a facial expression change and to only reflect the
       * former.
       * Is a 4x4 matrix and contains only the following components:
       *   * Uniform scale
       *   * Rotation
       *   * Translation
       * The last row is guaranteed to be `[0 0 0 1]`.
       * </pre>
       *
       * <code>optional .mediapipe.MatrixData pose_transform_matrix = 2;</code>
       */
      public Builder clearPoseTransformMatrix() {  copyOnWrite();
        instance.clearPoseTransformMatrix();
        return this;
      }

      // @@protoc_insertion_point(builder_scope:mediapipe.tasks.vision.face_geometry.proto.FaceGeometry)
    }
    @Override
    @SuppressWarnings({"unchecked", "fallthrough"})
    protected final Object dynamicMethod(
        MethodToInvoke method,
        Object arg0, Object arg1) {
      switch (method) {
        case NEW_MUTABLE_INSTANCE: {
          return new FaceGeometry();
        }
        case NEW_BUILDER: {
          return new Builder();
        }
        case BUILD_MESSAGE_INFO: {
            Object[] objects = new Object[] {
              "bitField0_",
              "mesh_",
              "poseTransformMatrix_",
            };
            String info =
                "\u0001\u0002\u0000\u0001\u0001\u0002\u0002\u0000\u0000\u0000\u0001\u1009\u0000\u0002" +
                "\u1009\u0001";
            return newMessageInfo(DEFAULT_INSTANCE, info, objects);
        }
        // fall through
        case GET_DEFAULT_INSTANCE: {
          return DEFAULT_INSTANCE;
        }
        case GET_PARSER: {
          com.google.protobuf.Parser<FaceGeometry> parser = PARSER;
          if (parser == null) {
            synchronized (FaceGeometry.class) {
              parser = PARSER;
              if (parser == null) {
                parser =
                    new DefaultInstanceBasedParser<FaceGeometry>(
                        DEFAULT_INSTANCE);
                PARSER = parser;
              }
            }
          }
          return parser;
      }
      case GET_MEMOIZED_IS_INITIALIZED: {
        return (byte) 1;
      }
      case SET_MEMOIZED_IS_INITIALIZED: {
        return null;
      }
      }
      throw new UnsupportedOperationException();
    }


    // @@protoc_insertion_point(class_scope:mediapipe.tasks.vision.face_geometry.proto.FaceGeometry)
    private static final FaceGeometry DEFAULT_INSTANCE;
    static {
      FaceGeometry defaultInstance = new FaceGeometry();
      // New instances are implicitly immutable so no need to make
      // immutable.
      DEFAULT_INSTANCE = defaultInstance;
      com.google.protobuf.GeneratedMessageLite.registerDefaultInstance(
        FaceGeometry.class, defaultInstance);
    }

    public static FaceGeometry getDefaultInstance() {
      return DEFAULT_INSTANCE;
    }

    private static volatile com.google.protobuf.Parser<FaceGeometry> PARSER;

    public static com.google.protobuf.Parser<FaceGeometry> parser() {
      return DEFAULT_INSTANCE.getParserForType();
    }
  }


  static {
  }

  // @@protoc_insertion_point(outer_class_scope)
}
