package com.google.mediapipe.tasks.vision.imagesegmenter;

import com.google.mediapipe.framework.image.MPImage;

import java.util.List;

// Generated by com.google.auto.value.processor.AutoValueProcessor
final class AutoValue_ImageSegmenterResult extends ImageSegmenterResult {

  private final List<MPImage> segmentations;

  private final long timestampMs;

  AutoValue_ImageSegmenterResult(
      List<MPImage> segmentations,
      long timestampMs) {
    if (segmentations == null) {
      throw new NullPointerException("Null segmentations");
    }
    this.segmentations = segmentations;
    this.timestampMs = timestampMs;
  }

  @Override
  public List<MPImage> segmentations() {
    return segmentations;
  }

  @Override
  public long timestampMs() {
    return timestampMs;
  }

  @Override
  public String toString() {
    return "ImageSegmenterResult{"
        + "segmentations=" + segmentations + ", "
        + "timestampMs=" + timestampMs
        + "}";
  }

  @Override
  public boolean equals(Object o) {
    if (o == this) {
      return true;
    }
    if (o instanceof ImageSegmenterResult) {
      ImageSegmenterResult that = (ImageSegmenterResult) o;
      return this.segmentations.equals(that.segmentations())
          && this.timestampMs == that.timestampMs();
    }
    return false;
  }

  @Override
  public int hashCode() {
    int h$ = 1;
    h$ *= 1000003;
    h$ ^= segmentations.hashCode();
    h$ *= 1000003;
    h$ ^= (int) ((timestampMs >>> 32) ^ timestampMs);
    return h$;
  }

}
